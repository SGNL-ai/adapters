version: "2"
linters:
  default: none
  enable:
    - asciicheck
    - decorder
    - dupl
    - dupword
    - durationcheck
    - forcetypeassert
    - goconst
    - godot
    - goheader
    - ineffassign
    - lll
    - makezero
    - misspell
    - nestif
    - nilerr
    - nlreturn
    - prealloc
    - predeclared
    - promlinter
    - reassign
    - revive
    # TODO Enable staticcheck
    # - staticcheck
    - unconvert
    - unparam
    - unused
    - usestdlibvars
    - wastedassign
    - wsl_v5
  settings:
    goheader:
      template: Copyright 2025 SGNL.ai, Inc.
    nestif:
      min-complexity: 10
    wsl_v5:
      allow-first-in-block: true
      allow-whole-block: false
      branch-max-lines: 5
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - dupl
        path: (.+)_test.go
      - linters:
          - wsl_v5
        path: (.+)_test.go
      - linters:
          - revive
        path: smoketests/common/
        text: "var-naming: avoid meaningless package names"
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
